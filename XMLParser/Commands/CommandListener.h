#pragma once
#include "../XML/XmlFile.h"
/**
 * @brief Обработва потребителски команди за работа с XML документ.
 *
 * Класът предоставя интерфейс за взаимодействие с потребителя чрез
 * конзолни команди и извиква съответните операции върху обект от тип XmlFile.
 */
class CommandListener {
public:

	/**
	 * @brief Стартира цикъл за обработка на потребителски команди.
	 *
	 * Приема команди от стандартния вход и ги изпълнява върху
	 * подадения XML документ.
	 *
	 * @param file XML документът, върху който ще се изпълняват командите.
	 */
	void run(XmlFile& file);
};